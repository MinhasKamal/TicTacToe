/**
 * @author Minhas Kamal(BSSE-0509, IIT, DU)
 */
/**
 * This is where the program starts from. This program has no special operation it just calls the main frame 
 * to start.
 */
package run;